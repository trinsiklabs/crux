# TDD Workflow
Every module built test-first. Tests written before implementation.
100% coverage enforced in pyproject.toml (fail-under=100).
Handler functions separate from MCP decorators for testability.
Tags: testing, tdd, workflow