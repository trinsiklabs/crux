just shipped repo-aware session logging for crux.

problem: working on multiple projects in one session. don't want crux content mixed with other work.

solution: hooks detect which repo you're editing. only logs to adopted projects.

edit ~/.crux/... → logged to Crux BIP
edit ~/repos/other/... → ignored

build in public, but keep the streams separate. #buildinpublic #opensource